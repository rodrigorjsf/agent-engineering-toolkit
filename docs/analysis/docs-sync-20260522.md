# Documentation Sync Report — Claude & Cursor (2026-05-22)

Comparison of the locally mirrored docs (`docs/claude/`, `docs/claude-code/`, `docs/cursor/`)
against current official documentation. Local mirrors were last captured ~2026-05-01;
drift window ≈ 3 weeks.

**Method:** 3 parallel `ce-framework-docs-researcher` subagents, one per tree, deep-compared
each local file against its upstream source. Fetch verified: `docs.claude.com` /
`platform.claude.com` and `code.claude.com/docs` serve clean raw markdown via the `.md`
suffix; `cursor.com/docs` is a Next.js SPA with no raw-markdown endpoint (WebFetch only).

**Strategy:** hybrid re-sync — refresh the official body, preserve project-added headers
and cross-references, fix lossy-capture degradation.

---

## Headline changes

| Tree | Files | Fresh (skip) | Re-sync | Flag-only | Deleted |
|------|-------|--------------|---------|-----------|---------|
| `docs/claude/` | 3 | 0 | 1 | 0 | 2 |
| `docs/claude-code/` | 9 | 1 | 7 | 1 | 0 |
| `docs/cursor/` | 13 | 4 | 7 | 2 | 0 |

The single biggest structural change: **Anthropic consolidated its entire per-technique
prompt-engineering page set into one page** (`claude-prompting-best-practices.md`). The slugs
`be-clear-and-direct`, `claude-4-best-practices`, and `claude-prompting-best-practices` all
now redirect to the same 62 KB page. Three local files mirrored it — see "Prompting
consolidation" below.

---

## docs/claude/ — Anthropic API / model docs

### Prompting consolidation (decided)

Three local files all mirror the one consolidated upstream page
`platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-prompting-best-practices.md`:

- `docs/claude/claude-interaction-guide.md` — **DELETED** (0 inbound references).
- `docs/claude/prompting-best-practices.md` — **DELETED** (1 inbound reference: `wiki/knowledge/log.md`).
- `docs/claude-code/claude-prompting-best-practices.md` — **survivor**, re-synced. Chosen
  because the project reference graph (compliance matrices, drift manifests, analysis
  files, wiki Sources, plugin docs — ~15 references, some in immutable completed PRPs)
  already points at this path; keeping it keeps every reference valid.

After deletion `docs/claude/` retains only `effective-harnesses-long-running-agents.md`.

### effective-harnesses-long-running-agents.md — re-sync (degraded-only)

- Upstream: `anthropic.com/engineering/effective-harnesses-for-long-running-agents` (blog, 2025-11-26).
- Body content is **fresh** — section structure, the `feature_list.json` example, the
  session transcript, the failure-modes table all match upstream.
- Degradation to fix: lossy capture **truncated the article tail** — the `Future work`,
  `Acknowledgements`, and `Footnotes` sections are missing; the footnote marker
  `structured updates.1` is orphaned. Curly typographic quotes throughout.

---

## docs/claude-code/ — Claude Code docs

Source: `code.claude.com/docs/en/*.md` (index at `code.claude.com/docs/llms.txt`).
No capture degradation found in this tree — all code fences balanced; staleness is purely
upstream content post-dating the capture.

### Re-sync (7 files)

- **`claude-prompting-best-practices.md`** — new top-level section **"Prompting Claude
  Opus 4.7"** (12 subsections: response verbosity, effort calibration, tool-use triggering,
  progress updates, literal instruction following, tone, subagent-spawn control, frontend
  defaults, interactive coding, code-review harnesses, computer use). Model lineup re-keyed
  to Opus 4.7 / 4.6 / Sonnet 4.6 / Haiku 4.5. Upstream is `platform.claude.com` API docs.
- **`hooks/claude-hook-reference-doc.md`** — largest drift (~760 new lines). **≥7 new hook
  events**: `Setup`, `UserPromptExpansion`, `PostToolBatch`, `PermissionDenied`,
  `TaskCreated`, `CwdChanged`, `FileChanged`. New top-level **Windows PowerShell tool**
  section. New `####` subsections (MCP tool hook fields, terminal notifications, add
  context, defer-a-tool-call).
- **`hooks/automate-workflow-with-hooks.md`** — new sections: "Reload environment when
  directory or files change", "Combine results from multiple hooks", "Hooks and permission
  modes", "Learn more". Expanded first-hook walkthrough; notification matcher table.
- **`memory/how-claude-remembers-a-project.md`** — new "When to add to CLAUDE.md" section;
  new **`CLAUDE.local.md`** local-instructions row; load-order table reordered
  broadest→specific; `/init` env flag `CLAUDE_CODE_NEW_INIT` now `=1`.
- **`plugins/claude-create-plugin-doc.md`** — new "Add background monitors to your plugin";
  "Submit to the official marketplace" renamed to "community marketplace"; new "Next steps".
- **`skills/extend-claude-with-skills.md`** — bundled-skills section restructured (prose +
  per-skill subsections, `/code-review` added); new intro paragraph. **Project header
  preserved**: the line-5 `**Related documentation**:` cross-reference block.
- **`subagents/claude-orchestrate-of-claude-code-sessions.md`** — mirror of the
  `agent-teams.md` page (title "Orchestrate teams of Claude Code sessions"); modest
  expansion, new "Use subagent definitions for teammates" section to verify.

### Fresh — no change (1 file)

- `subagents/creating-custom-subagents.md` — current (includes `xhigh` effort,
  `claude-opus-4-7` IDs, fork subagents, Task→Agent rename).

### Flag-only — no upstream 1:1 page (1 file)

- `skills/research-claude-code-skills-format.md` — project-authored research compilation
  (dated 2026-03-22), no single upstream page. Left untouched per the non-1:1 rule.
  **Possible internal drift to review later** (editorial, not a sync task): `effort` levels
  now include `xhigh`; the marketplace rename to "community marketplace"; the
  time-sensitive star counts / "Key Community Marketplace Repos" table.

---

## docs/cursor/ — Cursor IDE docs

Source: `cursor.com/docs/*` via WebFetch (SPA — no raw-markdown endpoint).

### Re-sync (7 files)

- **`hooks/hooks-guide.md`** — new **`workspaceOpen`** App-lifecycle hook event (fires on
  workspace open / folder change; output optional `pluginPaths`). New "Hook categories"
  framing (Agent / Tab / App-lifecycle hooks). Quickstart split into User vs Project hooks.
- **`plugin/plugin-full-reference.md`** — "Available hook events" now lists `workspaceOpen`
  under an "App lifecycle hooks" category. Otherwise current.
- **`plugin/plugins.md`** — new "Using the `workspaceOpen` hook" + "Hooks reference"
  sections; the "Using the Extension API" section was **removed upstream**. Degradation
  fix: line 9 "What plugins contain" table collapsed into one run-on line — rebuild as a
  proper 2-column table. **Project link preserved**: line-161 rewritten internal link to
  the sibling `plugin-full-reference.md` mirror.
- **`rules/rules.md`** — new "Glob pattern examples" subsection under Rule anatomy.
- **`skills/agent-skills-guide.md`** — new **`paths`** frontmatter field (comma-separated
  globs; legacy `globs` as fallback); new sections "Scoping a skill to specific files",
  "Nested skill directories", "Migrating rules and commands to skills" (`/migrate-to-skills`
  skill, v2.4). The local table over-specifies `license`/`compatibility` — not in current
  upstream; verify and likely drop. **Project block preserved**: lines 156-167 "Agent
  Skills standard" `docs/shared/skills-standard/` cross-reference.
- **`tools/terminal/terminal-guide.md`** — version-sensitive corrections: AppArmor package
  `0.4.0` → `0.6.0` in `.deb`/`.rpm` URLs (current URLs now 404); auto-run mode names
  changed ("Allowlist", "Allowlist (with Sandbox)", "Run Everything"); Linux now requires
  "Landlock v3"; "Default network allowlist" retitled "View default allowed domains".
- **`tools/worktrees/worktrees-guide.md`** — most restructured page: now leads with the
  Agents-Window workflow; new sections "Create a worktree in the Agents Window" and "How
  does Cursor discover existing worktrees?" (Cursor 3.5 checkpoint); `/worktree` /
  `/best-of-n` relocated under "Worktree Skills in Editor Window"; the old "previous
  parallel agents feature" comparison section removed; `cursor.worktreeMaxCount` default
  `20` → `25`.

### Fresh — no change (4 files)

- `mcp/mcp-guide.md`, `subagents/subagents-guide.md`, `tools/browser/browser-guide.md`,
  `tools/search/search-guide.md` — all current.
- Caveat for `subagents-guide.md`: WebFetch summarizers gave conflicting answers on whether
  the `model` field still documents `fast` — the update agent must verify the Model
  configuration table directly before any edit (none expected).

### Flag-only — non-1:1 by design (2 files)

- `best-practices/agent-best-practices.md` — explicit project summary of the cursor.com
  blog post (line 20 declares it intentionally non-1:1). Not re-synced. The upstream blog
  has grown (~30 subsections: Bugbot, Debug Mode, cloud agents + Slack, etc.) — optional
  future refresh of the bullet list, summary contract stands.
- `README.md` — project-authored directory index. File inventory still matches disk. After
  siblings re-sync, the one-line descriptions for hooks (App-lifecycle hooks) and worktrees
  (Agents Window) drift slightly — minor refresh only.

---

## Wiki propagation

Re-ingest via the `wiki-ingest` skill for wiki entries whose source doc had a **material
content change** (not for pure degradation fixes). Affected entries:

- `wiki/knowledge/claude-code-hooks.md` ← `automate-workflow-with-hooks.md`, `claude-hook-reference-doc.md`
- `wiki/knowledge/claude-code-memory.md` ← `how-claude-remembers-a-project.md`
- `wiki/knowledge/claude-code-plugins.md` ← `claude-create-plugin-doc.md`
- `wiki/knowledge/claude-code-skills.md` ← `extend-claude-with-skills.md`
- `wiki/knowledge/prompt-engineering.md` ← `claude-prompting-best-practices.md`
- `wiki/knowledge/cursor-hooks.md` ← `hooks-guide.md`
- `wiki/knowledge/cursor-plugins.md` ← `plugins.md`, `plugin-full-reference.md`
- `wiki/knowledge/cursor-rules.md` ← `rules.md`
- `wiki/knowledge/cursor-skills.md` ← `agent-skills-guide.md`
- `wiki/knowledge/cursor-tools.md` ← `terminal-guide.md`, `worktrees-guide.md`
- `wiki/knowledge/cursor-subagents.md` — source fresh, no re-ingest
- `wiki/knowledge/log.md` — drop the stale path reference to the deleted `docs/claude/prompting-best-practices.md`

---

## Consolidation bookkeeping (main thread)

- `git rm docs/claude/claude-interaction-guide.md docs/claude/prompting-best-practices.md`
- `docs/README.md` — reconcile entries for the consolidation.
- `wiki/knowledge/log.md` — fix the dangling reference to the deleted file.
- Out of scope / not touched: `docs/analysis/`, `docs/compliance/`, drift manifests,
  completed PRPs, plugin docs — these reference the surviving `claude-code/` file, which
  remains valid.
